import React from 'react';
import GetInTouchButton from './GetInTouchButton';

const WhatYouNeedIsNotListed = () => (
  <div className="call-to-action">
    <h3>What you need is not listed?</h3>
    <GetInTouchButton />
  </div>
);

export default WhatYouNeedIsNotListed;
